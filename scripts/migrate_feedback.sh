#!/bin/bash

# Move actions to the correct place
mv app/admin/feedback/actions.ts app/actions/feedback-actions.ts

# We already created feedback-actions.ts with getFeedback in the previous step? Let's check.
